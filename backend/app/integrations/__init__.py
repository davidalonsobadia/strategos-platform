"""External-system integration packages.

Each sub-package isolates the transport concerns of one external system behind a
port (an abstract client) so the rest of the application depends on a stable
contract rather than on the external API's shape.
"""
